name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
     - name: echo a string
       run: echo "First github action"
     - name: multiline script
       run: |
         node -v
         nmp -v
         jq -v
          